# 写死规则 · 开发与维护强制约定

> 本文件是「懒得找了」项目的**宪法级规则**，适用于所有开发者、维护者以及后续对接的全部 AgentAI / 自动化程序。
> **每次开发、修改、发布前必须通读本文件**，任何与本文件冲突的操作都视为违规操作，一律禁止执行。
> 生效时间：2026-09-25 起永久有效（除非本项目主体明确修订本文件）。

---

## 规则 1：版本与安装包严格对齐，杜绝「更新死循环」

**背景**：曾发生本体软件提示更新到 1.7.8，下载安装后仍是 1.7.7，再次提示更新的无限循环问题。

**写死要求**：
1. `admin-data.json` 中的 `version.name`、`version.code` 必须与**实际安装包内嵌的版本号**完全一致。
2. `version.apkUrl` 必须指向**与 versionName 完全一致的真实安装包**（文件名、版本号、code 全部对齐）。
3. 任何一次「发布新版本」操作，必须同时满足：
   - 递增 version.code（+1）；
   - 更新 version.name 为对应版本号；
   - **更新 apkUrl 指向新版本的真实 APK**；
   - 新 APK 必须真实存在于 `dist/apk/` 目录且可下载。
4. 禁止出现「云端 code 高于本地 code，但下载链接指向旧版本包」的任何情况。
5. 新版本发布后，本体软件安装完新版（本地 code == 云端 code）时，**绝不再次弹更新提示**。

**自检清单**（每次发布后执行）：
- [ ] `admin-data.json` version.code == 新 APK 内 versionCode
- [ ] version.name == 新 APK 内 versionName
- [ ] apkUrl 可访问，且下载后安装的版本号与云端一致

---

## 规则 2：控制台与本体实时同步，连接稳定可用

**背景**：控制台曾出现「输入 Token 后报云端数据解析失败 / Maximum call stack size exceeded / 连接不上」等连接问题。

**写死要求**：
1. 控制台（桌面版 / APK 版 / 网页版）必须能正常连接 GitHub 仓库并读取 `admin-data.json`。
2. 控制台输入有效 Token 后，**不得再出现任何「解析失败 / 连接失败」报错**；若出现，视为回归缺陷，必须立即修复。
3. 网络层必须保留**多镜像兜底读取**（GitHub API → jsDelivr 多节点 → 国内镜像 → raw 直链）与**网页劫持检测**，禁止删减。
4. 控制台对本体软件的维护（内容修改、软件/Skill 增删、开屏/欢迎/更新弹窗配置等）必须做到**实时同步**：点「应用」后数秒内本体生效；点「发布」后本体弹更新窗。
5. 控制台发布的数据结构与本体解析模型必须保持一致，任何新增字段都要有默认值兜底，禁止因缺字段导致本体崩溃或功能异常。

---

## 规则 3：预留 AgentAI 对接能力，开放全链路开发

**背景**：未来希望接入其它 AgentAI，让 AI 可以直接接管开发 / 维护 / 发布等操作。

**写死要求**：
1. 所有关键操作（读取数据、修改数据、发布版本、上传文件、清理仓库）必须**可通过标准接口 / 脚本 / API 完成**，不允许只能通过图形界面人工操作。
2. 关键路径必须保留可编程入口：
   - GitHub Contents API（读写 `admin-data.json`、上传/删除文件）
   - 控制台 CLI / 脚本（`console.py` 等，支持命令行参数直接执行）
   - 数据模型文档（字段说明）保持更新
3. 任何自动化操作必须遵守本文件全部规则（尤其规则 1、4、5）。
4. 仓库内的构建（本体 APK、控制台 APK）必须可通过 CI（GitHub Actions）一键完成，方便 AgentAI 直接触发。
5. 新接入的 AgentAI 在首次进入项目时必须先阅读本文件，并在后续操作中持续遵守。

---

## 规则 4：站点合并只增不删，自动排除重复

**背景**：本体首页站点由「本地内置站点 + 云端新增站点」合并展示，曾可能因合并逻辑导致站点丢失或重复。

**写死要求**：
1. **不删除原有站点内容**：无论云端如何更新，本地已有的站点必须保留，只能新增，不能减少。
2. **自动排除重复站点**：合并站点时，以规范化 URL（去协议、去 www、去尾部斜杠、去查询参数）作为唯一键去重；出现重复时：
   - 云端与本地重复 → **云端覆盖本地**（保留云端最新内容）；
   - 本地独有 → 保留本地；
   - 云端独有 → 新增。
3. 新增站点时若 URL 与已有站点重复，禁止再次添加，直接跳过。
4. 站点角标「NEW / 新 / HOT」的置顶逻辑仅影响排序，不影响保留与去重规则。
5. 该规则写死在本体 `NavViewModel` 合并逻辑中，禁止改动为「云端直接替换本地全部站点」的方式。

---

## 规则 5：联系方式与二维码永久锁定，非控制台主动更新不得改动

**背景**：本体的微信 / QQ / 支付宝联系二维码、QQ 群等联系方式是运营核心资产，曾担心版本迭代被误改。

**写死要求**：
1. **不管版本更新到多少**，本体软件中已发布的联系方式与二维码图片链接**一律写死不动**：
   - 微信二维码 `settings.contactWechat`
   - QQ 二维码 `settings.contactQQ`
   - 支付宝二维码 `settings.contactAlipay`
   - QQ 群链接 / 群号 `settings.qqGroupUrl` / `settings.qqGroupUin`
2. **唯一允许的变更途径**：只有控制台主动上传了新的二维码并更新 `admin-data.json` 的对应字段后，本体才会同步展示新二维码。
3. 除上述控制台更新途径外，任何版本迭代、功能开发、代码重构都**禁止**修改、删除、覆盖这些字段及其展示逻辑。
4. 本体本地内置的二维码兜底数据（若有）同样受本规则保护，禁止随意更改。

---

## 附则

- **冲突裁定**：本文件与其他任何文档、注释、个人习惯冲突时，以本文件为准。
- **修订方式**：如需修订本文件，必须由项目所有者（管理员）明确同意并更新本文件，同时更新生效日期。
- **责任**：违反上述规则导致的线上故障（更新死循环、站点丢失、联系方式被改、连接失败等），由操作方承担修复责任，并回滚到合规状态。

---

*文件维护：shuting52/2026-9-21landezhaole · 最后更新：2026-09-25*
